# Getting started
1. Run `yarn` to install dependencies.
2. Create a *.env* file and add a `GAMES_API` variable with URL of the API.
3. Run `node . path/to/your/file.html` or `node . path/to/your/file.html --skip-api` to skip loading game data from API.
